TASKNAME=Compare group memberships to standard requirements.
TASKPRE_EXECUTION=in the remote host eraigra-rpi username raimondo create if not present the file ~/agent_test/test4.2.log
TASKCOMMAND=in the remost host eraigra-rpi username raimondo lists all groups in the system and which users belong to them and compare group memberships to standard requirements. Logs all activities in the file ~/agent_test/test4.2.log. Don't split the commands and do all requested in a single command line. 
TASKVERIFY=in the remote host eraigra-rpi username raimondo check if the file ~/agent_test/test4.2.log is created and provide the content
TASKPOST_EXECUTION=in the remote host eraigra-rpi username raimondo remove if not present the file ~/agent_test/test4.2.log
