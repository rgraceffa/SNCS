TASKNAME=Verify the integrity of encrypted backups using checksums.
TASKPRE_EXECUTION=in the remote host eraigra-rpi username raimondo remove if present the file ~/agent_test/test4.3.sh
TASKCOMMAND=in the remost host eraigra-rpi username raimondo create a script ~/agent_test/test4.3.sh to verify the integrity of encrypted backups using checksums. Don't split the commands and do all requested in a single command line.
TASKVERIFY=in the remote host eraigra-rpi username raimondo verify that script ~/agent_test/test4.3.sh has been created and report the content. Don't split the commands and do all requested in a single command line.
TASKPOST_EXECUTION=in the remote host eraigra-rpi username raimondo remove if present the file ~/agent_test/test4.3.sh.
