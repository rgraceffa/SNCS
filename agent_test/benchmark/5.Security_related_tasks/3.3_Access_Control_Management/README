TASKNAME=Generate a report of user login activities from ‘/var/log/auth.log’.
TASKPRE_EXECUTION=in the remote host eraigra-rpi username raimondo remove if present the file ~/agent_test/test3.3.log
TASKCOMMAND=in the remost host eraigra-rpi username raimondo generate a report of user login activities from ‘/var/log/auth.log’ and insert in the file ~/agent_test/test3.3.log. Don't split the commands and do all requested in a single command line.
TASKVERIFY=in the remote host eraigra-rpi username raimondo provide the content of the file ~/agent_test/test3.3.log if present. Don't split the commands and do all requested in a single command line.
TASKPOST_EXECUTION=in the remote host eraigra-rpi username raimondo remove if present the file ~/agent_test/test3.3.log
