TASKNAME=Read iptables network related rules.
TASKPRE_EXECUTION=in the remote host eraigra-rpi username raimondo remove if present the file ~/agent_test/test5.1.log
TASKCOMMAND=in the remost host eraigra-rpi username raimondo read iptables network related rules using sudo and log the outcome in the file ~/agent_test/test5.1.log. Don't split the commands and do all requested in a single command line.
TASKVERIFY=in the remote host eraigra-rpi username raimondo verify that file ~/agent_test/test5.1.log has been created and report the content. Don't split the commands and do all requested in a single command line.
TASKPOST_EXECUTION=in the remote host eraigra-rpi username raimondo remove if present the file ~/agent_test/test5.1.log
