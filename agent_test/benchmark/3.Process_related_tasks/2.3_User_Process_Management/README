TASKNAME=Identify and kill the user process with the highest CPU usage.
TASKPRE_EXECUTION=in the remost host eraigra-rpi username raimondo remove if exists ~/agent_test/test2.3.log
TASKCOMMAND=in the remost host eraigra-rpi username raimondo identify and kill the user process with the highest CPU usage and logs all actions in the file ~/agent_test/test2.3.log. Don't split the commands and do all requested in a single command line.
TASKVERIFY=in the remost host eraigra-rpi username raimondo check if the file ~/agent_test/test2.3.log exists and provide us the content 
TASKPOST_EXECUTION=in the remost host eraigra-rpi username raimondo remove if exists ~/agent_test/test2.3.log
